[pytest]
testpaths = backend/tests
pythonpath = backend
# Geometry tests run the real CAD engine and are slow (~10s each, plus a
# one-off ~2 min on Windows while antivirus scans the OpenCascade DLLs).
markers =
    cad: needs the build123d/OpenCascade engine
